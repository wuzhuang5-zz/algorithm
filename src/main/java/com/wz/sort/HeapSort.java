package com.wz.sort;

/**
 * 堆排序
 */
public class HeapSort {
    public static void heapSort(int[] arr) {
        if (arr == null || arr.length < 2) {
            return;
        }

    }
    private static void bulidHeap(int[] arr) {
    }
}
